 # Movie Analysis Database Schema
 
 > Generated from `sql/schema.sql` (canonical) and `sql/db_backup.sql` (data dump).
 > Database: `movie_analysis` | Charset: `utf8mb4` | Collation: `utf8mb4_unicode_ci`
 
 ## Table List
 
 | # | Table | Source | Type | Description |
 |---|-------|--------|------|-------------|
 | 1 | movies | db_backup.sql | Core data | Movie metadata (id, title, genres) |
 | 2 | users | schema.sql | Core data | User profiles with demographic info |
 | 3 | ratings | schema.sql | Core data | User-movie ratings |
 | 4 | tags | db_backup.sql | Core data | User-generated movie tags |
 | 5 | top20_movies | db_backup.sql | Analysis result | Pre-computed top 20 movies (Task 1) |
 | 6 | gender_tag_stats | db_backup.sql | Analysis result | Gender-based tag statistics (Task 2) |
 | 7 | streaming_results | schema.sql | Analysis result | Real-time streaming aggregation (Task 3) |
 | 8 | streaming_stats | db_backup.sql | Analysis result | Streaming statistics (Task 3 variant) |
 
 ---
 
 ## 1. movies
 
 Core movie metadata from MovieLens dataset.
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | movieId | int | NOT NULL | - | Primary key, MovieLens movie ID |
 | title | varchar(500) | NOT NULL | - | Movie title (may include year in parentheses) |
 | genres | varchar(500) | YES | NULL | Pipe-delimited genres, e.g. "Crime|Drama|Thriller" |
 
 **Indexes:**
 - `PRIMARY KEY (movieId)`
 
 **Relationships:**
 - `movieId` referenced by: ratings, tags, top20_movies, streaming_results, streaming_stats
 
 ---
 
 ## 2. users
 
 User profiles with demographic information. Gender field enables gender-based analysis.
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | userId | INT | NOT NULL | - | Primary key, user ID |
 | gender | CHAR(1) | NOT NULL | - | Gender: M = Male, F = Female |
 | age | INT | YES | NULL | Age code (1-56, mapped to age ranges) |
 | occupation | INT | YES | NULL | Occupation code (0-20) |
 | zipCode | VARCHAR(10) | YES | NULL | Zip/Postal code |
 
 **Indexes:**
 - `PRIMARY KEY (userId)`
 
 **Relationships:**
 - `userId` referenced by: ratings, tags
 
 ---
 
 ## 3. ratings
 
 User-movie ratings from MovieLens dataset. Composite key prevents duplicate entries.
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | userId | INT | NOT NULL | - | Foreign key to users.userId |
 | movieId | INT | NOT NULL | - | Foreign key to movies.movieId |
 | rating | DECIMAL(2,1) | NOT NULL | - | Rating value: 0.5-5.0 in 0.5 increments |
 | timestamp | INT | NOT NULL | - | Unix timestamp of rating |
 
 **Indexes:**
 - `PRIMARY KEY (userId, movieId, timestamp)`
 - `INDEX idx_userId (userId)`
 - `INDEX idx_movieId (movieId)`
 
 **Relationships:**
 - `userId` → users.userId
 - `movieId` → movies.movieId
 
 ---
 
 ## 4. tags
 
 User-generated tags applied to movies. Source: MovieLens tags dataset.
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | userId | int | YES | NULL | Foreign key to users.userId |
 | movieId | int | YES | NULL | Foreign key to movies.movieId |
 | tag | varchar(500) | YES | NULL | User-generated tag text |
 
 **Indexes:**
 - `KEY idx_movieId (movieId)`
 - `KEY idx_userId (userId)`
 - `KEY idx_tag (tag(100))`
 
 **Relationships:**
 - `userId` → users.userId
 - `movieId` → movies.movieId
 
 ---
 
 ## 5. top20_movies
 
 Pre-computed top 20 highest-rated movies. Generated by Task 1 (Spark RDD).
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | rank | int | NOT NULL | - | Primary key, ranking position 1-20 |
 | movieId | int | YES | NULL | Foreign key to movies.movieId |
 | title | varchar(500) | YES | NULL | Movie title |
 | avg_rating | decimal(4,2) | YES | NULL | Average rating (rounded to 2 decimals) |
 | rating_count | int | YES | NULL | Number of ratings received |
 | updated_at | timestamp | YES | CURRENT_TIMESTAMP | Last update timestamp |
 
 **Indexes:**
 - `PRIMARY KEY (rank)`
 
 **Relationships:**
 - `movieId` → movies.movieId
 
 ---
 
 ## 6. gender_tag_stats
 
 Pre-computed gender-based tag statistics. Generated by Task 2 (Spark SQL).
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | gender | char(1) | NOT NULL | - | Part of composite PK, gender: M/F |
 | tag | varchar(100) | NOT NULL | - | Part of composite PK, movie tag/genre |
 | user_count | int | YES | NULL | Number of users of this gender interested in this tag |
 
 **Indexes:**
 - `PRIMARY KEY (gender, tag)`
 
 ---
 
 ## 7. streaming_results
 
 Real-time streaming aggregation results. Written by Task 3 (Spark Streaming).
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | id | BIGINT | NOT NULL | - | Primary key, auto-increment |
 | run_id | VARCHAR(64) | NOT NULL | - | Streaming job run identifier |
 | batch_time | DATETIME | NOT NULL | - | Timestamp of the batch |
 | movie_id | INT | NOT NULL | - | Foreign key to movies.movieId |
 | title | VARCHAR(255) | NOT NULL | - | Movie title (denormalized for query speed) |
 | avg_rating | DECIMAL(4,2) | NOT NULL | - | Cumulative average rating in this batch |
 | rating_count | INT | NOT NULL | - | Cumulative number of ratings |
 | created_at | TIMESTAMP | YES | CURRENT_TIMESTAMP | Record creation timestamp |
 
 **Indexes:**
 - `PRIMARY KEY (id)`
 - `INDEX idx_run_id (run_id)`
 - `INDEX idx_batch_time (batch_time)`
 - `INDEX idx_movie_id (movie_id)`
 
 **Relationships:**
 - `movie_id` → movies.movieId
 
 ---
 
 ## 8. streaming_stats
 
 Streaming statistics (alternative aggregation table).
 
 | Field | Type | Nullable | Default | Description |
 |-------|------|----------|---------|-------------|
 | batch_time | timestamp | NOT NULL | - | Part of composite PK, batch timestamp |
 | movieId | int | NOT NULL | - | Part of composite PK, foreign key to movies.movieId |
 | avg_rating | decimal(3,2) | YES | NULL | Average rating for this batch |
 
 **Indexes:**
 - `PRIMARY KEY (batch_time, movieId)`
 
 **Relationships:**
 - `movieId` → movies.movieId
 
 ---
 
 ## Entity-Relationship Diagram
 
 ```mermaid
 erDiagram
     movies {
         int movieId PK
         varchar title
         varchar genres
     }
 
     users {
         int userId PK
         char gender
         int age
         int occupation
         varchar zipCode
     }
 
     ratings {
         int userId PK
         int movieId PK
         decimal rating
         int timestamp
     }
 
     tags {
         int userId FK
         int movieId FK
         varchar tag
     }
 
     top20_movies {
         int rank PK
         int movieId FK
         varchar title
         decimal avg_rating
         int rating_count
         timestamp updated_at
     }
 
     gender_tag_stats {
         char gender PK
         varchar tag PK
         int user_count
     }
 
     streaming_results {
         bigint id PK
         varchar run_id
         datetime batch_time
         int movie_id FK
         varchar title
         decimal avg_rating
         int rating_count
         timestamp created_at
     }
 
     streaming_stats {
         timestamp batch_time PK
         int movieId PK
         decimal avg_rating
     }
 
     movies ||--o{ ratings : "has"
     movies ||--o{ tags : "has"
     movies ||--o{ top20_movies : "ranks"
     movies ||--o{ streaming_results : "streamed"
     movies ||--o{ streaming_stats : "streamed_stats"
     users ||--o{ ratings : "gives"
     users ||--o{ tags : "creates"
 ```
 
 ---
 
 ## Relationship Summary
 
 | Source Table | Target Table | Key | Cardinality | Description |
 |-------------|-------------|-----|-------------|-------------|
 | movies | ratings | movieId | 1:N | One movie has many ratings |
 | movies | tags | movieId | 1:N | One movie has many tags |
 | movies | top20_movies | movieId | 1:1 | One movie has one ranking |
 | movies | streaming_results | movieId | 1:N | One movie has many streaming records |
 | movies | streaming_stats | movieId | 1:N | One movie has many streaming stats |
 | users | ratings | userId | 1:N | One user gives many ratings |
 | users | tags | userId | 1:N | One user creates many tags |
 
 ---
 
 *Generated on 2025-06-17. Schema sources: sql/schema.sql, sql/db_backup.sql*
